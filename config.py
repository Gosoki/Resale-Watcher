"""配置。结构项（连库、端口、节流）走 .env，改了要重启；
业务规则（关键词、价格区间、排除词）在数据库 watch_rule 表里，面板上改，下一轮即时生效。
"""
import os
from datetime import datetime, timedelta, timezone
from pathlib import Path

from dotenv import load_dotenv

BASE_DIR = Path(__file__).resolve().parent
load_dotenv(BASE_DIR / ".env")

# 各平台的时间全是 JST，所有落库时间统一按 JST 写。
# 不用 MySQL 的 NOW()：数据库、开发机、部署机三边时区不保证一致。
JST = timezone(timedelta(hours=9))


def now() -> datetime:
    """当前 JST 时间，去掉 tzinfo —— MySQL 的 DATETIME 不存时区，带着反而会被驱动警告。"""
    return datetime.now(JST).replace(tzinfo=None)


def time_left(end) -> str:
    """距结束还有多久。拍卖里这是最要紧的一个数 —— 到点就没了。

    【为什么放在 config 而不是面板里】面板和推送两边都要写这个数，
    而 core/ 不能 import web/（那会把 nicegui 拖进抓取进程）。
    两边各写一份的话迟早分叉 —— 推送说「剩 180 分」、页面说「剩 3 小时」，
    人会以为是两个不同的时间。config 是两边本来就都 import 的那一个。
    """
    if not end:
        return ""
    sec = (end - now()).total_seconds()
    if sec <= 0:
        return "已结束"
    h = int(sec // 3600)
    if h < 1:
        return f"剩 {int(sec // 60)} 分"
    if h < 24:
        return f"剩 {h} 小时"
    return f"剩 {h // 24} 天 {h % 24} 小时"


def _int(key: str, default: int) -> int:
    try:
        v = int(os.getenv(key, "") or default)
    except ValueError:
        return default
    return v if v > 0 else default


# 默认值一律是无害占位：真实连接信息只存在于 .env（已在 .gitignore 里）。
# 这样万一 .env 丢了，进程会连本地失败并报错，而不是悄悄连上别的机器。
DB = {
    "host": os.getenv("DB_HOST", "127.0.0.1"),
    "port": _int("DB_PORT", 3306),
    "user": os.getenv("DB_USER", "root"),
    "password": os.getenv("DB_PASSWORD", ""),
    "database": os.getenv("DB_NAME", "resale_watcher"),
    # 【三个超时一个都不能少】pymysql 默认 read_timeout=None，是【无限】。
    # 2026-09-15 10:47 网络抖了一下，服务器把连接丢了，客户端那头的 socket
    # 还是 ESTABLISHED、卡在 recv() 里 —— 轮询线程就这么停了 40 小时：
    # 进程活着、面板开着、状态栏红字写着"轮询已停止"，但没人看面板。
    # 放 .env 而不是设置表：设置表本身要先连上库才读得到，鸡生蛋。
    "connect_timeout": _int("DB_CONNECT_TIMEOUT", 10),
    "read_timeout": _int("DB_READ_TIMEOUT", 30),
    "write_timeout": _int("DB_WRITE_TIMEOUT", 30),
}

WEB_PORT = _int("WEB_PORT", 2334)
WEB_HOST = os.getenv("WEB_HOST") or "127.0.0.1"

# ---- 可热改设置：{键: (类型, 默认值, 说明)} ----
# 这些【不进 .env】。建库时写进 app_setting 表，之后以数据库为唯一来源，
# 面板「设置」页改完 10 秒内生效。说明文字会一并写进表的 note 列，
# 所以用 Navicat / DBeaver 直接开表也看得懂每一项是干什么的。
SETTINGS_SPEC: dict[str, tuple[type, object, str]] = {
    # —— 面板显示 ——
    "fresh_hours": (
        int, 24,
        "【新上架】/【新发现】两个徽标的时间窗口（小时）。"
        "新上架＝商品本身挂出来不久（看平台给的上架时间）；"
        "新发现＝商品早就挂着，但我们刚把它抓进来（多半是它降价进了你的价格区间）。"
        "【ヤフオク 的搜索结果不给上架时间】所以它的商品只会有「新发现」，没有「新上架」。"
        "另外规则刚建起来的那一阵，库里所有东西都是刚抓到的，两个徽标会整个不显示 —— "
        "那时候全是新的，标了等于没标。",
    ),
    "stale_warn_hours": (
        int, 2,
        "商品有这么久没在搜索结果里出现过，就在命中页给它标一个「失联」。\n"
        "【为什么需要它】健康情况下每轮扫描都会重新看到在售商品（quick_min，7〜30 分钟），"
        "所以「几小时没见到」一定是出了事：多半是那个源在限流（实测 メルカリ 会回 403），"
        "对账拉不到详情，于是判不出它到底是卖掉了还是还挂着。\n"
        "这段时间里它照常显示在命中页上、照常有价格 —— 而那个价格是几小时前的。"
        "不标出来的话，你会对着一件可能早就卖掉的东西做判断。",
    ),
    "stale_hide_hours": (
        int, 24,
        "商品有这么久没在搜索结果里出现过，就【不在命中页显示了】（撤下，不是删除）。\n"
        "【为什么宁可撤下】命中页回答的是「现在有什么能买」。一条一天没更新过的记录"
        "回答不了这个问题，留着只会污染判断。\n"
        "【撤下不是丢掉】它还在库里，「全部」页照常看得到；只要它哪天重新出现在搜索结果里，"
        "last_seen_at 一刷新就自动回到命中页 —— 宁可让它重新入库，也不要拿旧数据糊弄你。\n"
        "撤下了几件会写在规则那一行的摘要里，不会偷偷藏起来。",
    ),
    # —— 市价基准 ——
    "median_window_days": (
        int, 30,
        "市价中位数只统计最近这么多天的成交。窗口外的成交样本会被自动删掉。"
        "实测 5090 全时段中位数 ¥590,100、近30天 ¥720,000——显卡在涨价，窗口开太大会失真。",
    ),
    "median_min_samples": (
        int, 5,
        "成交样本少于这么多件就不出中位数（面板显示「样本不足」，也不做捡漏判定）。"
        "实测搜 RTX 5090 的成交价从 ¥2,000 到 ¥998,000 都有，样本少时中位数会被一两件废品带偏。",
    ),
    # —— 抓取节奏（直接决定会不会被风控）——
    "req_delay_min": (float, 3.0, "两个 API 请求之间的最小随机等待（秒）。"),
    "req_delay_max": (float, 8.0, "两个 API 请求之间的最大随机等待（秒）。"),
    "search_reuse_min": (
        int, 5,
        "同一个源、同一个关键词的搜索页，如果【另一条规则】在这么多分钟内刚翻过，直接复用那一页，不再发请求。0＝关闭。\n"
        "【为什么】「RTX 5090 单卡」和「RTX 5090 整机」关键词一模一样，只是价格区间和词表不同 ——"
        "原先每条规则各翻一遍同样的两页，整机规则每 30 分钟白发一遍单卡规则 7 分钟前刚拿到的东西，"
        "一天约 500 次纯重复请求。\n"
        "【只复用别的规则翻的页】一条规则永远不会复用自己上一轮的页。\n"
        "【别调太大】复用来的页最多旧这么多分钟：整机规则 30 分钟一轮，复用一份 5 分钟前的页，"
        "新上架的整机最坏 35 分钟才被发现；调到 15 就是 45 分钟。省请求和早发现之间你自己拍。",
    ),
    "max_pages": (
        int, 5,
        "单条规则每轮最多翻几页（每页约 110 件）。实测「RTX 5090」全网在售仅 188 件、2 页扫完。"
        "只有当关键词宽到商品数超过 页数×110 时才会被截断，日志和面板都会告警——"
        "被截断时售出对账会自动跳过（没扫全的话「没出现」不等于「卖掉了」）。",
    ),
    "poller_lease_sec": (
        int, 600,
        "轮询租约有效期（秒）。多份实例（本机 + NAS）共用一个库时，只有拿到租约的那份"
        "真的去抓，其余待命；持有者每扫完一个源续一次期，停了（卡死、关机）到期后"
        "待命的那份自动接手。\n"
        "【为什么要这个】两份实例各自限速，同一个公网 IP 下对平台的请求密度翻倍，"
        "メルカリ 的 403 多半就是这么来的。\n"
        "要比「扫一个源最久要多久」长：被限流时一次退避就睡 300 秒，再加 5 页 × 8 秒 + "
        "10 个详情 × 8 秒 ≈ 2 分钟，600 留了将近一倍余量 —— 正被限流的时候换手，等于让第二份实例"
        "去撞同一堵墙。设太短会两边来回抢，设太长持有者死了要等更久才有人接。0＝不用租约，各抓各的。",
    ),
    "poller_stall_min": (
        int, 10,
        "轮询心跳停了这么多分钟，就往推送地址发一条「轮询已停」的告警（并写 ERROR 日志）。\n"
        "【为什么要这个】2026-09-15 轮询线程被一条卡死的数据库连接堵住，停了 40 小时 —— "
        "进程活着、面板开着、顶栏红字写着「轮询已停止」，但没人盯着面板。"
        "推送是唯一能主动找到你的通道。\n"
        "告警每个停机段只发一次，持续不恢复的话每 6 小时再提醒一次。0＝关闭。",
    ),
    "trading_recheck_min": (
        int, 180,
        "「交易中」的商品多久核实一次是否已成交（分钟）。对账和追踪刷新都按它来。\n"
        "【为什么单独一个数】交易中的商品不会出现在在售搜索里，所以每轮都算「失踪」，"
        "原先按 missing_grace_min 每 20 分钟拉一次详情 —— 而 メルカリ 从付款到确认成交常常要几天，"
        "实测稳态的失踪队列几乎全是交易中的，一天白拉上百次。",
    ),
    "sold_scan_hours": (int, 24, "成交轮间隔（小时）：多久抓一次成交价重算市价中位数。"),
    "detail_budget": (
        int, 10,
        "详情请求预算。【实际是「每个源、每个阶段」各一份】：售出对账花一份、"
        "补描述花一份，再乘以启用的源数 —— 三个源就是最多 6 倍这个数。"
        "稳态下每轮只有个位数新商品，远用不满；这个数是给「规则刚建立、一次涌进几十件」兜底的。",
    ),
    "missing_grace_min": (
        int, 20,
        "商品从搜索结果里消失多久之后（分钟）才去核实它是不是卖掉了。"
        "不设宽限期的话，分页抖动造成的单轮缺席会让每轮都多打十几个详情请求。",
    ),
    "daily_request_limit": (
        int, 3000,
        "每日请求上限保险丝：当天超过就停抓到次日 0 点（JST），"
        "而且是【所有规则所有源】一起停。\n"
        "估算公式：规则数 × Σ各源页数 × (1440 / quick_min) + 详情与成交轮。\n"
        "实测默认配置（2 条规则、3 个源、quick_min=7）：每轮 5 个搜索请求 × 约 197 轮/天"
        " × 2 条规则 ≈ 2000 次/天。\n"
        "【搜索只占一半，别照公式估】2026-09-13 实测 4 条规则（单卡×2 每7分钟 + "
        "整机×2 每30分钟）是 157 次/小时 ≈ 3770 次/天，而按页数算的搜索部分只有 106 次/小时。"
        "差出来的 50 次/小时是【售出对账】：每轮要对从搜索结果里消失的商品逐个拉详情核实，"
        "这是持续开销不是突发。所以估算时把公式算出来的数【乘以 1.5】才接近真实。\n"
        "【所以加第三条规则、或把 quick_min 从 7 调到 5，都会当天撞线】"
        "动这两个值之前先把上限一起调大。",
    ),
    # —— 追踪 ——
    "track_min": (
        int, 5,
        "追踪中的商品多久单独刷新一次（分钟）。\n"
        "【这是整个项目里唯一一处按件发请求的地方】平时商品的价格和状态只在整轮关键词"
        "扫描时更新（每条规则的 quick_min，默认 7 分钟，而且一轮要遍历所有源）；"
        "追踪的商品会直接拉它自己的详情页，所以快得多，拍卖的出价数也只有这条路能及时拿到。\n"
        "代价是每件每次刷新都是一个真实请求：追 10 件、间隔 5 分钟 ＝ 每天 2880 次，"
        "比现在四条规则的总量还大。【追之前先看看 daily_request_limit 还剩多少】",
    ),
    "track_budget": (
        int, 10,
        "每一轮最多刷新几件追踪商品。\n"
        "追的件数多过这个数时，按「最久没刷新的优先」轮着来 —— 不会漏掉谁，"
        "只是每件的实际间隔会被拉长。这道闸是防止你一口气追几十件就把配额烧穿。",
    ),

    # —— 推送 ——
    "notify_url": (
        str, "",
        "推送地址。【留空＝不推送，一个请求都不发】。\n"
        "填什么都行，常见的几家（都是实际接口形状）：\n"
        "  Slack     https://hooks.slack.com/services/T…/B…/…（模板见 notify_body）\n"
        "  ntfy      https://ntfy.sh/你的主题          （请求体模板留空）\n"
        "  Bark      https://api.day.app/你的KEY       （模板见 notify_body）\n"
        "  Discord   webhook 地址                      （模板见 notify_body）\n"
        "  企业微信  群机器人 webhook 地址             （模板见 notify_body）\n"
        "  Telegram  https://api.telegram.org/bot<TOKEN>/sendMessage\n"
        "【Slack 怎么拿这个地址】api.slack.com/apps → Create New App → From scratch →\n"
        "  选一个 workspace → 左侧 Incoming Webhooks → 打开开关 →\n"
        "  Add New Webhook to Workspace → 选要推到哪个频道 → 复制那串 https://hooks.slack.com/…\n"
        "【填完点设置页的「测试推送」】地址写错是【静默失败】的：发送出错只写日志，\n"
        "  面板上什么都不显示，你会一直以为推送开着。\n"
        "【这个地址等于一把钥匙】谁拿到都能往你手机推东西，别写进截图或仓库。",
    ),
    "notify_body": (
        str, "",
        "请求体模板（JSON）。留空＝把提醒正文当纯文本直接发（ntfy / Bark 这么用）。\n"
        "要发 JSON 就在这里写，用 {text} 占位提醒正文，会自动转义：\n"
        '  Slack     见下面「带图」那段\n'
        '  Discord   {"content": "{text}"}\n'
        '  企业微信  {"msgtype":"text","text":{"content":"{text}"}}\n'
        '  Telegram  {"chat_id":"你的chat_id","text":"{text}"}\n'
        "【Slack 就是上面那一行，照抄即可】商品链接放在正文最后一行，"
        "Slack 会抓那个页面的 og:image 展开成带图的预览卡片。\n"
        "【带图的 Slack 模板，照抄这一整行】\n"
        '  {"text": "{text}", "blocks": [{"type": "section", "text": {"type": "mrkdwn",'
        ' "text": "{text}"}, "accessory": {"type": "image", "image_url": "{thumb}",'
        ' "alt_text": "商品图"}}]}\n'
        "【{thumb} 是商品缩略图地址】我们不存图片，只存地址，图由 Slack 直接去源站 CDN 取。"
        "实测三个源的图都能被裸请求取到（不需要 Referer、不挡 Slackbot）。\n"
        "【顶层那个 text 不能省】它是手机通知栏和消息列表里显示的内容；"
        "只写 blocks 的话推送预览是一句「This content can't be displayed」。\n"
        "【靠链接自动展开是不行的】实测过：正文里放商品链接、甚至显式写上 "
        "unfurl_links/unfurl_media，Slack 都没有展开成带图卡片。要图就得自己塞。\n"
        "【没有缩略图的商品会自动退回纯文本】Slack 对空 image_url 回 400 invalid_blocks，"
        "整条消息会发不出去。见 core/notify.py 的 NO_THUMB_FALLBACK。\n"
        "标题里的引号和换行会自动转义，实测库里 637 条商品标题没有一条含 < 或 >"
        "（日文商品名用的是【】《》这类全角括号），所以不需要为 Slack 的 mrkdwn 另做转义。",
    ),
    "notify_on": (
        str, "deal",
        "推什么：deal＝只推捡漏（低于市价中位数 × deal_ratio%）；matched＝所有命中都推。\n"
        "【默认只推捡漏是有意的】命中只是「符合你的条件」，捡漏才是「该立刻去看」。"
        "所有命中都推的话，一条规则几十件在售会让你很快关掉推送。\n"
        "还没攒够成交样本时算不出中位数，也就没有捡漏 —— 那段时间填 deal 会一条都不推。",
    ),
    "notify_redrop_pct": (
        int, 0,
        "推过的商品比【上次推送时的价格】再跌够这么多百分比，就再推一条「📉 再降」。0＝关闭（每件只推一次）。\n"
        "【为什么默认关】原先一件商品终身只推一次；实测库里 26 条已推里有 5 条推完又降过价，"
        "你一条都不知道。开了的话建议填 5〜10：比的是上次推送时的价，每推一次基线刷新一次，"
        "所以掉几百块不会反复轰炸，但一路阴跌会每跌够一档响一次。\n"
        "只对定价商品有意义；拍卖只会涨。",
    ),
    "notify_final_min": (
        int, 60,
        "【拍卖快结束提醒】拍卖只剩这么多分钟、而价格还在捡漏线内时，再推一条。0＝关闭。\n"
        "【为什么要第二条】第一条是它刚变成捡漏那一刻发的 —— 那时可能还剩两天，"
        "两天足够你把它忘干净，而拍卖到点就没了，没有第二次机会。\n"
        "【只对拍卖生效】普通商品没有截止时间，晚一天去看它还在那儿。\n"
        "到点时价格已经被别人抬出捡漏线的【不发】—— 那就不是捡漏了。每件商品这条也只发一次。\n"
        "【要那会儿正在扫才发得出】提醒挂在每轮扫描的收尾上（因为捡漏判定是在那里重算的）。"
        "最后这段时间里当天配额耗尽、这条规则一次都没扫成，就不会发。",
    ),
    "notify_max_per_round": (
        int, 5,
        "一轮最多推几条。【超过就一条都不推，只把它们标成已推】——\n"
        "挡的是三种一次性井喷：刚填上 notify_url（库里几十件老命中一起轰出来）、"
        "停机几天后重启（攒下的过期货）、放宽规则或改价格区间（几百件同时变命中）。\n"
        "代价是真有一波好货时会被整批跳过，但那种情况面板上看得到；"
        "被几十条通知淹掉的人只会直接把推送关了。",
    ),
}

# 面板上怎么显示这些设置项：分组 → [(键, 中文名), ...]
#
# 【为什么不塞进 SETTINGS_SPEC 的元组里】那个元组被 store.py 在四处解包成
# (typ, default, note)，加一项就得同步改四处，而这里加的纯粹是界面文案，
# 跟取值、类型、落库都没关系。分开放，改 UI 文案不碰数据层。
#
# 【为什么中文名旁边还要留英文键】日志、报错、规则页的备注里引用的都是英文键
# （"先把 daily_request_limit 调大"），只显示中文名的话对不上号。
#
# 【顺序就是页面顺序】从"最常改的"到"基本不动的"排。
# 漏写一项会被 tests/test_settings_ui.py 拦下来 —— 漏了的表现是它在面板上
# 整个消失（不在任何分组里就不会被渲染），而库里的值照常生效，很难发现。
SETTING_GROUPS: dict[str, list[tuple[str, str]]] = {
    "推送": [
        ("notify_url", "推送地址"),
        ("notify_body", "请求体模板（JSON）"),
        ("notify_on", "推什么"),
        ("notify_redrop_pct", "推过的再跌多少 % 就再推（0=关）"),
        ("notify_final_min", "拍卖快结束再推一次（分钟，0=关）"),
        ("notify_max_per_round", "一轮最多推几条"),
    ],
    "面板显示": [
        ("fresh_hours", "「新上架/新发现」时间窗（小时）"),
        ("stale_warn_hours", "多久没见到就标「失联」（小时）"),
        ("stale_hide_hours", "多久没见到就从命中页撤下（小时）"),
    ],
    "追踪": [
        ("track_min", "追踪刷新间隔（分钟）"),
        ("track_budget", "每轮最多刷新几件"),
    ],
    "市价基准": [
        ("median_window_days", "中位数统计窗口（天）"),
        ("median_min_samples", "出中位数最少要几件成交"),
    ],
    "抓取节奏（直接决定会不会被风控）": [
        ("req_delay_min", "请求间隔下限（秒）"),
        ("req_delay_max", "请求间隔上限（秒）"),
        ("daily_request_limit", "每日请求上限（次）"),
        ("max_pages", "每轮最多翻几页"),
        ("search_reuse_min", "同关键词的搜索页复用（分钟，0=关）"),
        ("detail_budget", "详情请求预算（次）"),
        ("missing_grace_min", "失踪多久才去核实（分钟）"),
        ("trading_recheck_min", "交易中的多久核实一次（分钟）"),
        ("sold_scan_hours", "成交轮间隔（小时）"),
        ("poller_lease_sec", "多实例轮询租约（秒，0=各抓各的）"),
        ("poller_stall_min", "轮询停多久就告警（分钟，0=关）"),
    ],
}

LOG_DIR = BASE_DIR / "logs"
